fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
bcrypt>=4.0

# Driver do banco de produção -- instale o que corresponder ao DATABASE_URL
# escolhido (ver .env.example) quando for para produção. Não precisa dos
# dois; SQLite (usado localmente) não precisa de nenhum.
# psycopg2-binary>=2.9   # Postgres
# pymysql>=1.1           # MySQL
