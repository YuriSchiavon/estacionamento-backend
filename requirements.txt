fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
bcrypt>=4.0
psycopg2-binary>=2.9   # Postgres -- necessário em produção (Railway/Render), ver DATABASE_URL no .env.example
qrcode>=7.4            # gera o QR code do ticket impresso no totem de entrada (fábrica SVG, sem precisar de Pillow)

# Driver alternativo, só se o banco de produção escolhido for MySQL em vez de Postgres:
# pymysql>=1.1
